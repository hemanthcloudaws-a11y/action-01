name: my first workflow

on: push

jobs: 
    first_job:
        runs-on: ubuntu-latest
        steps: 
            - name: Welcome message
              run: echo "my first github action"

            - name: list files
              run: ls
              
            - name: read files
              run: cat README.md
